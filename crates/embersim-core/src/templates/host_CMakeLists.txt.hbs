# Auto-generated by EmberSim. Do not edit manually.
cmake_minimum_required(VERSION 3.14)
project({{project_name}}_host LANGUAGES C)

set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)

# Compiler flags
if(UNIX)
    add_compile_options(-Wall -Wextra -g)
elseif(WIN32)
    add_compile_options(/W4 /Zi)
endif()

# Include mocks directory
include_directories("${CMAKE_CURRENT_SOURCE_DIR}/mocks")

# User application sources (relative to project root, from embersim.json)
set(USER_SOURCES
{{#each user_sources}}
    "../{{this}}"
{{/each}}
)

# EmberSim runtime sources
set(EMBERSIM_SOURCES
    "mocks/mock_hal.c"
    "mocks/trace_log.c"
    "mocks/mock_state.c"
    "mocks/mock_uart.c"
    "mocks/mock_i2c.c"
    "mocks/mock_spi.c"
    "mocks/mock_tim.c"
    "mocks/ember_sim_kernel.c"
    "mocks/ember_regs.c"
    "host_main.c"
)

# Combined executable
add_executable(embersim_host ${USER_SOURCES} ${EMBERSIM_SOURCES})

# Optional coverage
if(COVERAGE)
    target_compile_options(embersim_host PRIVATE --coverage)
    target_link_options(embersim_host PRIVATE --coverage)
endif()